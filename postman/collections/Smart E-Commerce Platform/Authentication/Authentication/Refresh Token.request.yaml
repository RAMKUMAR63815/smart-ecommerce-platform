$kind: http-request

name: Refresh Token

description: ""

url: "{{baseUrl}}/auth/refresh"

method: POST

headers:
  - key: Content-Type
    value: application/json
  - key: Accept
    value: application/json

body:
  type: json
  content: |-
    {
      "refresh_token": "{{refresh_token}}"
    }

settings: {}

examples: ./.resources/RefreshToken.resources/examples

order: 1003